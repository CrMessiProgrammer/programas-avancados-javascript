function soma(n1=0, n2=0) {         //n1=0 e n2=0 são parâmetros já pré-definidos, para se caso 
    return n1 + n2                  //não passar valor algum para eles, ambos valerão 0
}
console.log(soma(2, 5))